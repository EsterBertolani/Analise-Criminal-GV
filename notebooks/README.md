# Notebooks

Esta pasta pode receber notebooks exploratórios, mas o MVP final foi organizado em scripts reprodutíveis dentro de `src/` e `scripts/`.

Sugestão:

- mantenha notebooks apenas para experimentação;
- mova funções finais para `src/`;
- use `scripts/run_pipeline.py` como caminho oficial de reprodução.
